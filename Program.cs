using System;

namespace Inicio_com_c_
{
    class Program
    {
        static void Main(string[] args)
        {
            // var s = new Pilha();
            // s.Empilha(1);
            // s.Empilha(10);
            // s.Empilha(100);
            // Console.WriteLine(s.Desempilha());
            // Console.WriteLine(s.Desempilha());
            // Console.WriteLine(s.Desempilha());


            // String[] argumento = {};
            // Tipos.Declaracoes();
            // Tipos.InstrucaoIf(argumento);
            // Tipos.InstrucaoSwitch(argumento);

            Ponto p1 = new Ponto(0,0);
            Ponto3D p2 = new Ponto3D(10, 20, 30);
            
            Ponto3D.Calcular();
        }
    }
}
